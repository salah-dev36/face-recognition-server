const { handleSignIn } = require("./sign-in");
const { handleSignUp } = require("./sign-up");
const { recognize } = require("./recognize");

module.exports = { handleSignIn, handleSignUp, recognize };
